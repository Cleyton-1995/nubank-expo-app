import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
   marginTop: 0,
   marginHorizontal: 30
  },
  code: {
   backgroundColor: "#FFFFFF",
   padding: 10,
  },
});